# 决策记录 / Decisions

记录本项目**已经做过的非平凡架构/设计决定**，以及"为什么选 X 而不选 Y"。

## 为什么需要这份文件

agent 没有项目记忆。如果某个选择已经讨论清楚（"用 PostgreSQL 不用 SQLite"、"消息走 SSE 不走 WebSocket"、"配置走环境变量不写文件"），不记下来下次 agent 会重新提议另一种方案，浪费一轮甚至改坏。

worker 在开始任务前会读本文件。协调者写 spec 前也会先扫这份。

## 什么进、什么不进

**进**：

- 架构层选择（语言、框架、存储、消息、部署形态）
- 通用约定（接口风格、错误处理范式、命名规则）
- 已经否决过的方案 + 否决理由
- 跨多个任务都要参考的规则

**不进**：

- 实现细节（变量名、函数签名级别的事——代码本身是真相）
- 一次性的临时绕过（属于 error-journal）
- 还在讨论中没拍板的事

## 格式

每条按时间倒序写一段，包含 5 行：

```
## YYYY-MM-DD: <一句话决定>

- 背景：<为什么要做这个选择>
- 选项：<考虑过的几种方案>
- 决定：<选了哪个>
- 理由：<为什么选这个，关键 trade-off>
- 影响：<对哪些代码/流程的约束>
```

## 示例

```
## 2026-01-15: 任务队列存 SQLite 而非 Redis

- 背景：需要进程间可靠的任务队列
- 选项：(1) SQLite WAL 文件队列；(2) Redis 队列；(3) Postgres LISTEN/NOTIFY
- 决定：SQLite WAL
- 理由：单机部署，无外部依赖，崩溃恢复天然有事务；Redis 多一个进程
- 影响：所有 backend 写队列走 src/harness/db.py；数据库文件不可放 NFS
```

---

<!-- 在此追加新条目 -->
